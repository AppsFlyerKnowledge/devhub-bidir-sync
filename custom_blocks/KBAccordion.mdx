---
name: KBAccordion
---
import React, { useState } from 'react';

export const KBAccordion = ({ children, icon, iconColor, title }) => {
  const [isOpen, setIsOpen] = useState(false);

  return (
    <div className="border border-gray-200 dark:border-gray-600 rounded-md overflow-hidden">
      <div
        className={`flex items-center justify-between w-full p-4 text-base font-medium bg-gray-50 dark:bg-gray-800 cursor-pointer hover:bg-gray-100 dark:hover:bg-gray-700 ${isOpen ? 'border-b border-gray-200 dark:border-gray-600' : ''}`}
        onClick={() => setIsOpen(!isOpen)}
      >
        <span className="flex items-center">
          {icon && (
            <i className={`fa-duotone fa-solid ${icon} mr-2`} style={{ color: iconColor }} />
          )}
          <span>{title}</span>
        </span>
        <i className={`fa fa-regular fa-chevron-right text-sm text-gray-500 dark:text-gray-300 transition-transform ${isOpen ? 'rotate-90' : ''}`} />
      </div>
      {isOpen && (
        <div className="text-gray-600 dark:text-white bg-white dark:bg-gray-900 px-4 pt-4 pb-5">
          {children}
        </div>
      )}
    </div>
  );
};

<KBAccordion title="Accordion one">
Lorem ipsum
</KBAccordion>